-use a fun style on your map, also different marker. something opaque? 
-Remove small infowindow from mobile devices
-use css for hamburger icon

We might want to turn off the handler for uploadTask after uploading the image.  

make sure the stillResizing thing is implemented and working

make sure we don't get in trouble with uploading images with same name (change name with affix?)

add edit button, and delete button in largeInfoWindow
add remove image button

Make loading screen untill all places are loaded

get loading screen ('please don't close window'), when importing json (and deleting all images)


Synchronous XMLHttpRequest on the main thread is deprecated because of its detrimental effects to the end user's experience. For more help, check https://xhr.spec.whatwg.org/.


setlistheight when resize screen (like android bug)
